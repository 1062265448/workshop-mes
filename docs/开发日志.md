# 开发日志 - 2026 年 4 月

---

## 2026-04-14 上午

### 09:00 - 10:00 | 平面库配货模块 v1.3.2 → 重构启动

**背景：** 配货模块原有代码耦合严重，需重构为基于 `inventory_stock` 新表的规范化架构。

**分 4 步执行（防崩溃）：**

| 步骤 | 内容 | 临时 Tag |
|------|------|---------|
| 1/4 | 数据库 Schema：新增 `inventory_stock`、`distribution_order_item` 表 | v1.3.2.1 |
| 2/4 | 后端重写：Service/Controller/DTO，RESTful 规范化（/order→/orders） | v1.3.2.2 |
| 3/4 | 前端 API 层 + RecognitionHistory.vue 重构（351→142行） | v1.3.2.3 |
| 4/4 | 前端 DistributionOrder.vue 主页面重构（减少约 50% 代码量） | v1.3.2.4 |

### 10:00 - 11:30 | Bug 修复 + UI 调试

**修复的 Bug：**

| Bug | 原因 | 修复方式 |
|-----|------|---------|
| 新建配货单库存列表空白 | `customerId` 空字符串触发 `ParseIntPipe` 400 错误 | 新增 `OptionalIntPipe` |
| 图片上传不触发 | `el-upload` `auto-upload="false"` | 改为 `auto-upload="true"` |
| AI 识别超时 | axios 默认 30s，Qwen VL API 需 60s+ | `timeout: 120000` |
| axios multipart 解析失败 | 手动设置 `Content-Type: multipart/form-data` 缺 boundary | 移除手动设置 |

**测试数据：** 插入 3 个客户（华东金属、南方镍业、北方新材料）+ 5 条库存记录（B20260414001-005），用于 UI 调试。

### 11:30 - 11:48 | Git 版本整理 + GitHub 推送

**问题：** GitHub 连接不稳定（无法访问 github.com:443），多次重试后成功。

**版本整理：** 合并中间提交（v1.3.2.1 ~ v1.3.2.4 + bug fix），统一为 `v1.3.3` 单一提交 + 开发日志提交。

**最终提交结构：**
- `v1.3.3` (96153e2) — 平面库配货模块全面重构完成
- (df68940) — 添加开发日志

**推送状态：** ✅ master + v1.3.3 tag 已推送到 GitHub

### 重构成果总结

| 维度 | 重构前 | 重构后 | 改善 |
|------|--------|--------|------|
| 库存表 | `inbound_record`（入出库共用） | `inventory_stock`（专用表） | 结构清晰 |
| 配货单表 | 缺少明细表 | `distribution_order_item` | 数据规范化 |
| 后端 API | 路径不规范（/order） | RESTful（/orders） | 标准化 |
| DTO 验证 | 无 | 完整 DTO + class-validator | 安全性提升 |
| 前端页面 | 约 600 行 | 约 300 行 | 减少 50% |
| 识别历史 | 约 351 行 | 约 142 行 | 减少 60% |
| 超卖风险 | 无事务 | `$transaction` 锁定库存 | 数据安全 |

---

## 2026-04-13 回顾

| 时间 | 内容 | Tag |
|------|------|-----|
| 上午 | 会议模块开发完成 + 数据看板 | v1.3.2 |
| 下午 | 生产管理模块 v1.3.1 提交 | v1.3.1 |
